<div class="row flex-lg-row-reverse align-items-center g-5 py-5">
  <div class="col-10 col-sm-8 col-lg-6">
    <img src="/img/food.jpg"  class=" rounded-5 d-block mx-lg-auto  img-fluid" alt="Bootstrap Themes" width="607" height="510" loading="lazy">
  </div>
  <div class="col-lg-6">
    <h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">Responsive left-aligned hero with image</h1>
    <p class="lead">Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most popular front-end open source toolkit, featuring Sass variables and mixins, responsive grid system, extensive prebuilt components, and powerful JavaScript plugins.</p>
    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
      <a href="/explore-latest" type="button" class="btn btn-primary btn-lg px-4 me-md-2">Explore Latest</a>
      <a heref="/random-recipe" type="button" class="btn btn-outline-secondary btn-lg px-4">Random</a>
    </div>
  </div>
</div>
<!-- category Start -->
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
  <% if (categories != 'undefined') { %>
   <% categories.forEach(category=> { %>
    <a href="/categories/<%= category.name %>" class="col text-center category__link">
      <div class="category__image shadow">
        <img src="/img/<%= category.img %>" alt="<%= category.img %>" loading="lazy">
      </div>
      <div class="pt-1"><%= category.name %></div>
    </a>
   <% }) %>
  <% } %>
  <a href="/categories" class="col text-center category__link">
    <div class="category__image shadow">
      <img src="/img/americanfood.jpg" alt="" loading="lazy">
    </div>
    <div class="pt-1">View All</div>
  </a>
</div>
<!-- category end -->
<!-- section latest start -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h1>Latest Recipe</h1>
    <a href="/explore-latest" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if (food != '') { %>
      <% food.latest.forEach(recipe => { %>
        <a href="/recipe/<%= recipe._id %>"class="col text-center category__link">
          <div class="category__image category__image--large shadow">
            <img src="/upload/<%= recipe.image %>" alt="<%= recipe.name %>">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% } %>
  </div>
</section>
<!-- section latest end -->

<!-- thai start -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h1>Thai Recipe</h1>
    <a href="/categories/thai" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if (typeof food != 'undefined' && food.thai.length >0) { %>
      <% food.thai.forEach(recipe => { %>
        <a href="/recipe/<%= recipe._id %>"class="col text-center category__link">
          <div class="category__image category__image--large shadow">
            <img src="/upload/<%= recipe.image %>" alt="<%= recipe.name %>">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% }  else { %>
      <p>Not item found</p>
    <% }  %>
  </div>
</section>
<!-- thai end -->
<!-- American start -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h1>American Recipe</h1>
    <a href="/categories/american" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if (typeof food != 'undefined' && food.american.length >0) { %>
      <% food.american.forEach(recipe => { %>
        <a href="/recipe/<%= recipe._id %>"class="col text-center category__link">
          <div class="category__image category__image--large shadow">
            <img src="/upload/<%= recipe.image %>" alt="<%= recipe.name %>">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% }  else { %>
      <p>Not item found</p>
    <% }  %>
  </div>
</section>
<!-- american end -->
<!-- chinese start -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h1>Chinese Recipe</h1>
    <a href="/categories/chinese" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if (typeof food != 'undefined' && food.chinese.length >0) { %>
      <% food.chinese.forEach(recipe => { %>
        <a href="/recipe/<%= recipe._id %>"class="col text-center category__link">
          <div class="category__image category__image--large shadow">
            <img src="/upload/<%= recipe.image %>" alt="<%= recipe.name %>">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% }  else { %>
      <p>Not item found</p>
    <% }  %>
  </div>
</section>
<!-- chinese end -->
<!-- submit section -->
  <section class="px-4 py-5 my-5 text-center">
    <img src="/img/publish.png" class="d-block mx-auto img-fluid" alt="" loading="lazy" width="566" height="208">
    <h1 class="display-5 fw-bold">Publish your recipe for free today</h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead mb-4">Publish your recipe in front of thounds people free today</p>
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
        <a href="" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a>
      </div>
    </div>
  </section>
<!-- submit section end -->